// export const HomePage = lazy(() => new Promise(resolve => setTimeout(() => resolve( import('./pages/_HomePage') ), 3000)))
// export const lazyload = (path, time) => lazy(() => new Promise(resolve => setTimeout(() => resolve( import(path) ), time)))
// TEST
