import { css } from 'styled-components'

export const test = css`
	html, body, #root { border: 3px solid darkslategray; }
	#root * { border: 2px solid silver; }
	#root * { transition: all 0.25s linear; }
`
